# Archived Nuxt Template

This directory contains all the original Nuxt template files (in `nuxt-template/`) provided for UTD projects exactly as is. It is being archived because we decided on using a MERN stack instead.

The contents of this `archive/` directory are not a part of the PawGo project.
